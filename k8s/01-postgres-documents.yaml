apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-documents-pvc
  namespace: docker-minio
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-documents-config
  namespace: docker-minio
data:
  POSTGRES_DB: documents_db
  POSTGRES_USER: docuser

---
apiVersion: v1
kind: Secret
metadata:
  name: postgres-documents-secret
  namespace: docker-minio
type: Opaque
stringData:
  POSTGRES_PASSWORD: docpass123

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-documents
  namespace: docker-minio
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-documents
  template:
    metadata:
      labels:
        app: postgres-documents
    spec:
      containers:
      - name: postgres
        image: postgres:16-alpine
        ports:
        - containerPort: 5432
        envFrom:
        - configMapRef:
            name: postgres-documents-config
        - secretRef:
            name: postgres-documents-secret
        volumeMounts:
        - name: data
          mountPath: /var/lib/postgresql/data
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          exec:
            command:
            - /bin/sh
            - -c
            - pg_isready -U docuser
          initialDelaySeconds: 30
          periodSeconds: 10
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: postgres-documents-pvc

---
apiVersion: v1
kind: Service
metadata:
  name: postgres-documents
  namespace: docker-minio
spec:
  selector:
    app: postgres-documents
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
  type: ClusterIP
